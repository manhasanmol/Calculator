first of all install express using npm install express
after that also you will need to one time install of
 nodemon in one pc.
 create file using npm init
 to use html elements attribute you will need
 body-parse so install it also 